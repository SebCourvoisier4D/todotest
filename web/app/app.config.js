todoApp
	.config(function ($wakandaConfigProvider) {
		/*
		 * Change the address according to your production settings
		 */
		//var hostname = 'http://yourdomain.com:8081';
		//$wakandaConfigProvider.setHostname(hostname);
		
	});